services:
  blog:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: telegram-blog
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - blog_data:/app/data

volumes:
  blog_data:
